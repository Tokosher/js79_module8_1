export function squareOfQuadrate (side) {
  return Math.pow(side, 2);
}

export function squareOfTriangle (side1, side2) {
  return side1 * side2 / 2;
}

export function squareOfRectangle (side1, side2) {
  return side1 * side2;
}